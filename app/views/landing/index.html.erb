<div class="centered">
  <table class="display-table">
    <tr>
      <th>Name</th>
      <th>Planted</th>
      <th>Harvested</th>
      <th>Active</th>
      <th>Comments</th>
      <th></th>
    </tr>
  <% @garlic.each do |g| %>
    <tr>
      <td><%= g.name %></td>
      <td><%= g.planted %></td>
      <td><%= g.harvested %></td>
      <td><%= g.active %></td>
      <td><%= g.comments %></td>
      <td><%= link_to 'Edit',
        edit_garlic_path(g),
        class: 'btn btn-success'
      %>
      <%= link_to 'Delete',
        garlic_path(g),
        method: :delete,
        data: { confirm: 'Are you sure?' }
      %></td>
    </tr>
  <% end %>
  <% @redonion.each do |ro| %>
    <tr>
      <td><%= ro.name %></td>
      <td><%= ro.planted %></td>
      <td><%= ro.harvested %></td>
      <td><%= ro.active %></td>
      <td><%= ro.comments %></td>
      <td><%= link_to 'Delete',
        redonion_path(ro),
        method: :delete,
        data: { confirm: 'Are you sure?' }
      %></td>
    </tr>
  <% end %>
  <% @mango.each do |m| %>
    <tr>
      <td><%= m.name %></td>
      <td><%= m.planted %></td>
      <td><%= m.harvested %></td>
      <td><%= m.active %></td>
      <td><%= m.comments %></td>
      <td><%= link_to 'Delete',
        mango_path(m),
        method: :delete,
        data: { confirm: 'Are you sure?' }
      %></td>
    </tr>
  <% end %>
  <% @whiteonion.each do |wo| %>
    <tr>
      <td><%= wo.name %></td>
      <td><%= wo.planted %></td>
      <td><%= wo.harvested %></td>
      <td><%= wo.active %></td>
      <td><%= wo.comments %></td>
      <td><%= link_to 'Delete',
        whiteonion_path(wo),
        method: :delete,
        data: { confirm: 'Are you sure?' }
      %></td>
    </tr>
  <% end %>
  </table>
</div>

<div id="tempdata">
  <iframe width="600" height="371" seamless frameborder="0" scrolling="no" src="https://docs.google.com/spreadsheets/d/1SQGJ60F3YhEF4o05Ht9jsWr4DgF0hFVYhK3xeEoHtls/pubchart?oid=1340289642&amp;format=interactive"></iframe>
</div>
